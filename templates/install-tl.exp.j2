#!/usr/bin/expect
spawn  sudo {{ tlinstalldir }}/tl-{{ tlversion }}-server/install-server
expect "Press Enter to continue..."
send "\r"
expect "Press Enter to continue..."
send "\r"
expect "Press Enter to continue..."
send "\r"
expect "?"
send "no\r"
expect eof